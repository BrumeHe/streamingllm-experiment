## 实验结果与分析

### 1 Attention sink 的可视化证据

四张热力图（`results/sink_heatmap_ctx{2048,8192}_layer{1,26}.png`）呈现清晰的**浅层 vs 深层模式分化**：

- **浅层（layer 1）**：注意力几乎完全集中在**局部对角线带**上——每个 token 主要关注自己附近的少量 token，这是典型的"局部语法/位置敏感"行为，开头位置并无特殊。
- **深层（layer 26）**：热力图左边缘出现一条醒目的**竖直亮线**，对应所有 query 都把大量注意力投给最开头的几个 key；右侧逐列平均曲线的粉色区域（前 4 个 token）出现高出其余位置 2–3 个数量级的尖峰。

这正是 attention sink 的直观证据：深层注意力不再依赖局部结构，过剩的注意力质量被系统性地倾倒在序列开头。

### 2 三种缓存策略的 PPL 对比

| 策略 | 说明 | PPL | 相对 full |
|---|---|---|---|
| full | 完整 KV（基线） | 4.4726 | — |
| window | 纯滑窗（最近 1024） | 8.5696 | **+91.6%** |
| sink_window | 前 4 sink + 最近 1024 | 4.7126 | **+5.4%** |

（原始数据：`results/ppl_compare.csv`）

### 3 逐块 PPL 曲线（`results/ppl_per_chunk.csv`）

| chunk 起点 | full | window | sink_window |
|---|---|---|---|
| 0 | 6.02 | 6.02 | 6.02 |
| 1024 | 4.32 | 4.32 | 4.32 |
| 2048 | 5.78 | **16.33** | 5.88 |
| 4096 | 5.21 | **13.48** | 5.47 |
| 8192 | 4.77 | **11.38** | 5.34 |
| 11264 | 5.43 | **9.94** | 6.13 |

三个观察：

1. **前两块三种模式完全相同**（PPL 6.02 / 4.32）：此时序列未超过窗口长度，没有发生任何截断。这反过来验证了评测实现的正确性——差异纯粹由缓存策略引起。
2. **纯滑窗从第 2048 块（sink 被丢弃的时点）起显著恶化**，PPL 长期维持在 8–16，约为基线的 2–3 倍。这证明开头几个 token 一旦从 KV 中移除，模型输出分布即被破坏，且损伤是**永久性**的（后续块不会恢复）。
3. **sink+滑窗全程紧贴基线**（平均仅差 5.4%）：4 个 sink token 几乎零成本地修复了纯滑窗的崩坏，缓存却从线性增长变为恒定大小（本例省约 91.7% 的 KV 显存）。

### 4 讨论

- **sink 的本质**：开头 token 语义上并不重要，但 softmax 必须归一化，模型学会了把"无处可去的注意力"倾倒给它们。sink token 是注意力的"排水口"，不是信息的"仓库"——这解释了为何 RoPE 位置失真（sink 与新 token 距离极远）对 sink_window 影响不大，而删掉它们则后果严重。
- **对部署的意义**：StreamingLLM 以常数级 KV 显存支持理论上无限长的流式输入，代价仅是约 5% 的 PPL 上升，对实时对话、持续日志处理等场景极具工程价值。
- **局限性**：本实验只用单文档、单模型规模、单组窗口参数；sink_window 在个别块上（如 11264）仍与基线有可见差距，说明窗口外信息的损失并未被 sink 完全补偿。

## 结论

1. 在 Qwen2.5-1.5B 上复现了 attention sink 现象：**深层的注意力质量系统性地汇聚在序列最开头的 2–4 个 token 上**，而浅层呈局部对角带模式。
2. 纯滑动窗口截断会摧毁模型输出分布（PPL +91.6%），损伤从 sink 被丢弃的时点开始且不可逆。
3. StreamingLLM 式"sink + 滑窗"策略以 4 个 token 的代价将 PPL 恶化收窄到 +5.4%，同时把 KV cache 从线性增长降为恒定大小，实验结果与论文的核心论断一致。